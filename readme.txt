build:
1) build.bat/build.sh to build the zip package in build folder

deploy:
1) extract the zip package to working folder

run:
1) go to working folder
2) run.bat/run.sh to start the application



